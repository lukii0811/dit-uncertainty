# Unmasked baseline

Creates one reference using the fixed parameters from `../config.json` and
`keep_ratio=1.0`. Every latent state and decoded image is saved.

```bash
../../../.venv/bin/python run.py
```
