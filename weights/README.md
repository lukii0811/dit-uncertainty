# Model weights

- `0400000.pt` — TREAD SiT-B/2 training checkpoint used by the experiments.
- `huggingface/hub/` — local Hugging Face cache for the
  `stabilityai/sd-vae-ft-ema` VAE and `facebook/dinov2-base`.
- `torch/hub/checkpoints/alexnet-owt-7be5be79.pth` — AlexNet feature weights
  used by LPIPS v0.1.

Experiment outputs must not be stored in this directory.
